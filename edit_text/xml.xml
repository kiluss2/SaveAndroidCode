// search bar without bottom line
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/llSearch"
    android:layout_width="match_parent"
    android:layout_height="46dp"
    android:background="@drawable/bg_round_corner_search"
    android:gravity="center_vertical"
    android:paddingStart="16dp">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_search" />

    <EditText
        android:id="@+id/edtSearch"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:layout_weight="95"
        android:background="@null"
        android:hint="@string/search_for_accounts"
        android:inputType="text"
        android:textSize="14dp" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/ivCloseSearchBar"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground"
        android:paddingHorizontal="10dp"
        android:src="@drawable/ic_close_2"
        android:tint="@color/brandeis_blue"
        android:visibility="gone" />

</LinearLayout>
